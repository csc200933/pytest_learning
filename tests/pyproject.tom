[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers --cov=src --cov-report=term-missing --cov-report=xml"
markers = [
  "unit: fast tests without DB",
  "integration: tests with dynamodb (moto/local)",
]

[tool.coverage.run]
omit = ["src/repo_dynamo.py"]