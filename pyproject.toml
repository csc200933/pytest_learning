[project]
name = "pytest_learning"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "boto3",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "pytest-cov",
  "pytest-mock",
  "moto[boto3]",
]